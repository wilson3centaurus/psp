<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit Teacher | PSP</title>
  <link rel="stylesheet" href="/css/style.css">
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css" rel="stylesheet">
  <link rel="icon" type="image/png" href="/assets/logo.png">
</head>
<body class="form-page">
  <div class="form-page-container">
    <div class="form-page-header">
      <a href="/teacher" class="back-btn"><i class="fas fa-arrow-left"></i></a>
      <h1><i class="fas fa-edit"></i> Edit Teacher</h1>
      <button class="theme-toggle" aria-label="Toggle theme">
        <i class="fas fa-moon"></i>
        <i class="fas fa-sun"></i>
      </button>
    </div>

    <div class="card form-card">
      <form action="/teacher/edit/<%= teacher.id %>" method="POST">
        <div class="form-group">
          <label for="name"><i class="fas fa-user"></i> Full Name</label>
          <input type="text" id="name" name="name" class="form-input" value="<%= teacher.name %>" required>
        </div>

        <div class="form-row">
          <div class="form-group">
            <label for="subject"><i class="fas fa-book"></i> Subject</label>
            <select id="subject" name="subject" class="form-input" required>
              <option value="">Select Subject</option>
              <option value="English" <%= teacher.subject === 'English' ? 'selected' : '' %>>English</option>
              <option value="Mathematics" <%= teacher.subject === 'Mathematics' ? 'selected' : '' %>>Mathematics</option>
              <option value="Science" <%= teacher.subject === 'Science' ? 'selected' : '' %>>Science</option>
              <option value="Social Studies" <%= teacher.subject === 'Social Studies' ? 'selected' : '' %>>Social Studies</option>
              <option value="Shona" <%= teacher.subject === 'Shona' ? 'selected' : '' %>>Shona</option>
              <option value="Ndebele" <%= teacher.subject === 'Ndebele' ? 'selected' : '' %>>Ndebele</option>
              <option value="Physical Education" <%= teacher.subject === 'Physical Education' ? 'selected' : '' %>>Physical Education</option>
              <option value="Agriculture" <%= teacher.subject === 'Agriculture' ? 'selected' : '' %>>Agriculture</option>
              <option value="Art" <%= teacher.subject === 'Art' ? 'selected' : '' %>>Art</option>
              <option value="Music" <%= teacher.subject === 'Music' ? 'selected' : '' %>>Music</option>
              <option value="Religious Studies" <%= teacher.subject === 'Religious Studies' ? 'selected' : '' %>>Religious Studies</option>
              <option value="ICT" <%= teacher.subject === 'ICT' ? 'selected' : '' %>>ICT</option>
              <option value="Life Skills" <%= teacher.subject === 'Life Skills' ? 'selected' : '' %>>Life Skills</option>
              <option value="Other" <%= !['English','Mathematics','Science','Social Studies','Shona','Ndebele','Physical Education','Agriculture','Art','Music','Religious Studies','ICT','Life Skills'].includes(teacher.subject) ? 'selected' : '' %>>Other</option>
            </select>
          </div>
          <div class="form-group">
            <label for="gender"><i class="fas fa-venus-mars"></i> Gender</label>
            <select id="gender" name="gender" class="form-input" required>
              <option value="">Select Gender</option>
              <option value="Male" <%= teacher.gender === 'Male' ? 'selected' : '' %>>Male</option>
              <option value="Female" <%= teacher.gender === 'Female' ? 'selected' : '' %>>Female</option>
              <option value="Other" <%= teacher.gender === 'Other' ? 'selected' : '' %>>Other</option>
            </select>
          </div>
        </div>

        <div class="form-row">
          <div class="form-group">
            <label for="phone"><i class="fas fa-phone"></i> Phone</label>
            <input type="text" id="phone" name="phone" class="form-input" value="<%= teacher.phone %>" required>
          </div>
          <div class="form-group">
            <label for="email"><i class="fas fa-envelope"></i> Email</label>
            <input type="email" id="email" name="email" class="form-input" value="<%= teacher.email %>" required>
          </div>
        </div>

        <div class="form-group">
          <label for="teacher_id"><i class="fas fa-id-badge"></i> EC Number</label>
          <input type="text" id="teacher_id" name="teacher_id" class="form-input" value="<%= teacher.teacher_id %>" required>
          <small class="form-hint"><i class="fas fa-info-circle"></i> Employee Code Number assigned by the ministry</small>
        </div>

        <div class="form-actions">
          <a href="/teacher" class="btn outline"><i class="fas fa-times"></i> Cancel</a>
          <button type="submit" class="btn primary"><i class="fas fa-save"></i> Update Teacher</button>
        </div>
      </form>
    </div>
  </div>

  <script src="/js/main.js"></script>
</body>
</html>
