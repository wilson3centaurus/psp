<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Resources | PSP</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="/css/style.css">
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css" rel="stylesheet">
  <link rel="icon" type="image/png" href="/assets/logo.png">
</head>
<body>
  <div class="app-shell">
    <div class="sidebar-overlay"></div>

    <aside class="sidebar">
      <div class="sidebar-header">
        <div class="sidebar-brand">
          <img src="/assets/logo.png" alt="Logo" class="sidebar-logo">
          <div>
            <div class="sidebar-brand-text"><%= user.username %></div>
            <div class="sidebar-brand-label">School Portal</div>
          </div>
        </div>
      </div>
      <nav class="sidebar-nav">
        <div class="nav-section">
          <div class="nav-section-title">Main</div>
          <a class="nav-link" href="/school/dashboard"><i class="fas fa-gauge-high"></i> Dashboard</a>
        </div>
        <div class="nav-section">
          <div class="nav-section-title">Management</div>
          <a class="nav-link" href="/student"><i class="fas fa-user-graduate"></i> Students</a>
          <a class="nav-link" href="/teacher"><i class="fas fa-chalkboard-user"></i> Teachers</a>
          <a class="nav-link active" href="/resources"><i class="fas fa-boxes-stacked"></i> Resources</a>
        </div>
        <div class="nav-section">
          <div class="nav-section-title">Attendance</div>
          <a class="nav-link" href="/student-attendance"><i class="fas fa-clipboard-user"></i> Student Attendance</a>
          <a class="nav-link" href="/teacher-attendance"><i class="fas fa-user-clock"></i> Teacher Attendance</a>
        </div>
        <div class="nav-section">
          <div class="nav-section-title">Account</div>
          <a class="nav-link logout" href="/logout"><i class="fas fa-right-from-bracket"></i> Logout</a>
        </div>
      </nav>
    </aside>

    <main class="main-content">
      <header class="topbar">
        <div class="topbar-left">
          <button class="mobile-menu-btn" aria-label="Toggle menu">
            <i class="fas fa-bars"></i>
          </button>
          <span class="page-title">Resources</span>
        </div>
        <div class="topbar-right">
          <button class="theme-toggle" aria-label="Toggle theme">
            <i class="fas fa-moon"></i>
            <i class="fas fa-sun"></i>
          </button>
          <a href="/logout" class="topbar-btn danger">
            <i class="fas fa-sign-out-alt"></i>
            <span>Logout</span>
          </a>
        </div>
      </header>

      <section class="content">
        <div class="page-header">
          <div>
            <h1><i class="fas fa-boxes-stacked"></i> School Resources</h1>
            <p class="subtitle">Manage per-subject resource counts and upload via CSV.</p>
          </div>
          <a href="/resources/add" class="btn primary"><i class="fas fa-plus"></i> Add Resource</a>
        </div>

        <% if (success_msg && String(success_msg).trim() !== "") { %>
          <div class="alert success"><i class="fas fa-check-circle"></i> <%= success_msg %></div>
        <% } %>
        <% if (error_msg && String(error_msg).trim() !== "") { %>
          <div class="alert error"><i class="fas fa-exclamation-circle"></i> <%= error_msg %></div>
        <% } %>

        <div class="card">
          <div class="card-header">
            <h3 class="card-title"><i class="fas fa-upload"></i> Upload Resources</h3>
          </div>
          <div style="padding: 0.5rem 0;">
            <a href="/uploads/sample-templates/resource_template.csv" download style="font-size: 0.8rem; margin-bottom: 0.5rem; display: inline-block; opacity: 0.7;"><i class="fas fa-download"></i> Download sample template</a>
            <form action="/resources/upload-csv" method="POST" enctype="multipart/form-data">
              <label for="res_csv" style="display: block; margin-bottom: 0.4rem; font-size: 0.85rem; opacity: 0.8;">Choose CSV file (columns: subject_id, subject_name, grade, num_students, num_books, num_computers)</label>
              <div style="display: flex; gap: 0.5rem; align-items: center; flex-wrap: wrap;">
                <input type="file" id="res_csv" name="csv" accept=".csv" class="form-input" required style="flex: 1; min-width: 200px;">
                <button class="btn secondary" type="submit"><i class="fas fa-upload"></i> Upload CSV</button>
              </div>
            </form>
          </div>
        </div>

        <div class="card">
          <div class="card-header">
            <h3 class="card-title"><i class="fas fa-list"></i> Resource List</h3>
            <span class="badge"><%= resources ? resources.length : 0 %> resources</span>
          </div>
          <div class="table-wrapper">
            <table>
              <thead>
                <tr>
                  <th>#</th>
                  <th>Subject ID</th>
                  <th>Subject Name</th>
                  <th>Grade</th>
                  <th>Students</th>
                  <th>Books</th>
                  <th>Computers</th>
                  <th>Actions</th>
                </tr>
              </thead>
              <tbody>
                <% if (!resources || !resources.length) { %>
                  <tr><td colspan="8" class="empty-state">No resources added yet.</td></tr>
                <% } else { %>
                  <% resources.forEach((res, i) => { %>
                    <tr>
                      <td><%= i + 1 %></td>
                      <td><code><%= res.subject_id %></code></td>
                      <td><strong><%= res.subject_name %></strong></td>
                      <td><span class="badge"><%= res.grade %></span></td>
                      <td><%= res.num_students %></td>
                      <td><%= res.num_books %></td>
                      <td><%= res.num_computers %></td>
                      <td>
                        <div class="table-actions">
                          <a href="/resources/edit/<%= res.id %>" class="btn icon outline" title="Edit"><i class="fas fa-edit"></i></a>
                          <form action="/resources/delete/<%= res.id %>" method="POST" onsubmit="return confirm('Delete this resource?')" class="inline">
                            <button type="submit" class="btn icon danger" title="Delete"><i class="fas fa-trash"></i></button>
                          </form>
                        </div>
                      </td>
                    </tr>
                  <% }) %>
                <% } %>
              </tbody>
            </table>
          </div>
        </div>
      </section>

      <footer class="footer">
        © 2026 Primary School Performance System
      </footer>
    </main>
  </div>

  <script src="/js/main.js"></script>
</body>
</html>
